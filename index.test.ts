import { Hyperplane } from "./index";

describe("hyperplane-typescript-sdk", () => {
    it("initialize client", async () => {
        const hyperplane = new Hyperplane({
            apiKey: "API_KEY",
        });
    });
});
